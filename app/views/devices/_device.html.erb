<li class="<%= device.available? ? "available" : "unavailable" %>">
  <%= link_to device_path(device) do %>
    <h3><%= device.model %></h3>
    <div class="photo">
        <%= device_image_tag(device) %>
    </div>
  <% end %>
</li>